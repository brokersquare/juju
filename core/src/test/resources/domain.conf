akka {
  #loglevel = "DEBUG"
  loglevel = "WARNING"
  persistence {
    journal.plugin = "akka.persistence.journal.inmem"
    snapshot-store.plugin = "akka.persistence.snapshot-store.local"
  }

  test.default-timeout = 20s
}

juju {
  timeout = 500s
  eventbus.timeout = 500s
}

